/**
 * 容十三内水s是说
 */
import aaa from './index'
const bbb = require('./test.js'),
  ccc = 'ccc==mock.key.com======'
var method = 'fangfa====='
const a = { a: 1, b: 2 }
async function run(aaa: string) {
  await testing(
    {
      url: 'url mock.key.com ===========',
      method,
      param: {
        aaa,
        bbb
      },
      ...aaa
    },
    'run ==========='
  )
  test2(ccc) // ingggggggg
}
